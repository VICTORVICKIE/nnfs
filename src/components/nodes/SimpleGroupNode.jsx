import './NodeStyles.css';

export default function SimpleGroupNode({ data }) {
  return (
    <div className="simple-group-node">
      {/* No header, no controls, just a container */}
    </div>
  );
}
